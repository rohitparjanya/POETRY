<li>
        <p>
            <% if (locals.user && locals.user.id == comment.user.id){ %>
            <small>
                <a href="/comments/destroy/<%= comment.id %>"><i class="fas fa-window-close"></i></a>
            </small>
            <% } %>
            <%= comment.content %>
            <small>
                - <%= comment.user.name %>
            </small>
        </p>    

</li>